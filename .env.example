# Direct public intake is deliberately fail-closed. Use a fine-grained token
# limited to this repository with Issues: write, then enable the service.
REPAIR_SUBMISSION_ENABLED=false
GITHUB_REPAIR_TOKEN=
